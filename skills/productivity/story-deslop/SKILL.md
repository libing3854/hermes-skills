---
name: story-deslop
version: 2.0.0
description: |
  网文去AI味。检测并清除文本中的AI写作痕迹，让文字回归自然、非模板化。
  v2.0: 整合 Humanizer 技能的 29 种 AI 模式，新增 Gate G 深层模式清除（意义膨胀、刻意换词、宣传性语言、虚假范围、过度限定、先行预告、灵魂注入），新增 50 分制质量评分框架。
  触发方式：/story-deslop、/去AI味、「去AI味」「去味」「deslop」「这篇太AI了」
metadata:
  hermes:
    related_skills: [humanizer]
  openclaw:
    source: https://github.com/worldwonderer/oh-story-claudecode
---

# story-deslop：网文去AI味

你是网文润色专家。你的任务是把 AI 味浓重的网文文本改写自然，降低模板化、书面腔和过度工整感。

**核心信念：AI 味的主要问题不是语法，而是过度圆滑、工整、解释充分。改写目标是保留剧情功能，同时增加口语、停顿、跳跃和具体动作。**

---

## ⚠️ Pitfall: patch工具逐条替换更可靠

章节润色需要大量文本替换。**不要用多hunk的patch**——如果其中一个hunk匹配失败，整个patch被拒绝，所有修改都丢失。

**正确做法**：逐条调用 `patch(mode='replace')`，每次只做一处替换。虽然调用次数多，但每条独立成功/失败，不会互相影响。

实战教训（第243章）：首次尝试用一个大patch包含20处修改，全部因第3个hunk匹配失败而回滚。改为逐条replace后全部成功。

---

## ⚠️ Pitfall: 中文字数统计方法

中文字数有三种统计方式，结果差异可达30%+。用户说"压缩到X字"时，必须先确认用哪种计数方式，否则会反复调整：

| 统计方法 | Python代码 | 说明 |
|----------|-----------|------|
| 纯中文字 | `len(re.findall(r'[\u4e00-\u9fff]', text))` | 只算汉字，不含标点 |
| 中文+中文标点 | `len(re.findall(r'[\u4e00-\u9fff\u3000-\u303f\uff00-\uffef]', text))` | 汉字+中文标点，**网文平台最常用** |
| 所有非空白字符 | `len(re.sub(r'\s', '', text))` | 含ASCII字母、数字、英文标点 |

**实战经验**：同一段文字，纯中文字4,023 vs 中文+中文标点4,517 vs 非空白字符4,780。目标是4,500-5,500时，纯中文字可能不达标但中文+中文标点已达标。

**压缩/扩展时的坑**：
- 第一次压缩宁可少删，不要过头。过头后补回来的内容风格容易不一致
- 建议先统计当前字数，计算需要删减/增加的比例，再逐段操作
- 每次修改后立即重新统计，不要改完全部再统计

---

## 核心哲学

### 原则 1：不是改错，是改味

AI味不是语法错误，不需要"修正"。AI味是一种风格问题——过于书面化、过于对仗工整、过于面面俱到。去AI味的本质是把文字从过度工整拉回具体、自然、可读。

### 原则 2：改最少，效果最大

去AI味不是重写。目标是改最少的字，让整段文字的"味"变过来。能改一个词就不改一句，能删一句就不重写一段。没有问题的句子尽量保留原句；人名、地名、数字、章节名、专有名词优先保留。

**过度去AI味保护**：
- **不得整段删除正文内容**。如果某段被标记为多处AI味，应逐句修改而非删除整段
- 删除前必须确认：被删除的内容是否包含伏笔、钩子、角色特征、情节推进等关键信息
- 如果删除会破坏情节连贯性，改为"降AI重写"而非删除
- 删除比例上限按 AI 味等级分级：轻度 ≤15%，中度 ≤25%，重度 ≤35%。重度文本可通过“合并重复描写+重写降AI”产生更大字符差，但仍不得整段删除或删掉剧情功能。超过对应比例应在报告中标记超限风险，并输出分段处理方案
- 如果逐句修改后某段仍不满意，在去AI味报告中标注 `[需复核]` 而非删除，不计入 15% 上限
- 对于"疑似AI味但不确定"的内容，在去AI味报告中标注 `[需复核]`，而非插入正文

### 原则 3：保留创作意图

去AI味只改"怎么说"，不改"说什么"。剧情、人设、情节走向一概不动；不新增原文没有的情节、设定、关系或时间线。如果原文有逻辑问题，那不是去AI味的活。

---

## 自然文本基准

去AI味需要知道自然网文文本的特征。以下是从热门网文中提炼的非模板化写作特征，作为对比基准：

### 自然文本特征（与AI味对比）
| 维度 | 自然文本 | AI味文本 |
|------|----------|--------|
| 段落长度 | 1-3句为主，偶尔1句独占1行 | 每段4-6句，整齐均匀 |
| 对话标签 | 60%+无标签，用动作替代"说" | 几乎每句都有"说道/问道" |
| 情绪表达 | 动作展示（"手在抖"） | 直接告诉（"很紧张"） |
| 比喻 | 生活化（"像哈士奇护食"） | 文学化（"如寒冰般"） |
| 语气词 | "嘤""嘶""靠""行吧" | 几乎没有 |
| 省略 | 大量省略，读者自己脑补 | 面面俱到，生怕读者不懂 |
| 排比 | 偶尔1-2个，从不连续3+ | 连续3-5个排比是标配 |
| 结尾 | 动作/对话收尾 | 总结/升华/感慨收尾 |

### 自然表达替换参考
> 来自大量网文写作研究：

- 替代"深吸一口气"→ "胸口起伏了一下" / 直接删掉
- 替代"眼中闪过一丝..."→ "他垂下眼" / "眯起眼"
- 替代"嘴角勾起一抹..."→ "笑了一下，没到眼底" / "乐了"
- 替代"仿佛..."→ "像..." / 直接白描
- 替代"不禁..."→ 直接写动作
- 替代"缓缓开口"→ "说" / 用动作引出对话

---

## 检测流程

### Phase 1：AI味扫描

对用户提交的文本做快速扫描，标记AI味浓重的位置：

```
## AI味检测报告

### 整体评估
- AI味等级：{轻度/中度/重度}
- 主要问题：{1-3 个关键词}

### 问题标记
| 位置 | 类型 | 原文 | 问题 |
|------|------|------|------|
| 第X段 | 禁用词 | "眼中闪过一丝..." | 典型AI高频词 |
| 第Y段 | 句式 | "...，带着..." | AI惯用句式 |
| 第Z段 | 节奏 | 连续3句排比 | 过于工整 |
| ... | 心理描写 | "他感到..." | 告诉而非展示 |
| 第N段 | 重复描写 | 同一动作连续拆写 | 相邻段重复同一瞬间 |
| 第P段 | 意义膨胀 | "标志着..." | 普通事件被拔高（Gate G1） |
| 第Q段 | 刻意换词 | 同一角色3种称呼 | 同义词循环（Gate G2） |
| 第R段 | 宣传腔 | "充满活力的..." | 推销式描写（Gate G3） |
```

---

### Phase 2：诊断与分级

根据 Phase 1 检测结果判断AI味程度，决定处理策略：

| AI味程度 | 量化标准（参考值） | 特征 | 处理策略 |
|----------|---------|------|----------|
| 轻度 | 禁用词命中 ≤5 处/千字，无连续 3+ 句式套路 | 少量禁用词，偶有书面腔 | 只过 Gate A + B |
| 中度 | 禁用词命中 6-15 处/千字，或有连续 3+ 句式套路 | 多处禁用词 + 句式套路 + 心理描写抽象 | 过 Gate A + B + C + D |
| 重度 | 禁用词命中 >15 处/千字，或 6 Gate 中 4+ 个有问题 | 全文AI味明显，节奏/对话/结尾都有问题 | 完整 6 Gate + 重点段落重写 |

### 实战数据：大莉（deepseek-v4-pro）写作的 AI味 水平

使用 `deepseek-v4-pro` 通过 delegate_task 批量写作 12 章网文（共约 6.6 万字）后，story-deslop 扫描结果为：
- 禁用词密度：0.1 处/千字（轻度）
- 主要问题：高频词「轻轻」「突然」「微微」等共 9 处
- 修复方式：逐处替换即可，无需 6 Gate 全流程

**结论**：大莉输出的文本 AI味 极低，只需做轻度禁用词替换。写完后若用户要求"检查AI味"，直接执行 Gate A（禁用词替换）+ Gate C（重复描写差异化）即可，无需加载完整 6 Gate 流程。

| 指标 | 计算方式 | 轻度阈值 | 中度阈值 | 重度阈值 |

> 量化标准为参考值。命中 = banned-words.md 中条目的精确匹配（非子串），同一词在一处出现计 1 次。如果计数与主观判断冲突，以定性描述为最终裁定依据。

**AI味打分客观指标**：

| 指标 | 计算方式 | 轻度阈值 | 中度阈值 | 重度阈值 |
|------|----------|---------|---------|---------|
| 禁用词密度 | 命中次数 / 千字 | ≤5 | 6-15 | >15 |
| 连续排比段数 | 连续相同句式结构的段落数 | ≤2 | 3-4 | ≥5 |
| 心理词占比 | 直接心理描写词数 / 总段落数 | ≤10% | 10-25% | >25% |
| 对话标签密度 | "说道/问道/笑道" 等 / 对话句数 | ≤30% | 30-50% | >50% |
| 平均段落句数 | 总句数 / 总段落数 | ≤3 | 3-5 | >5 |
| 重复描写密度 | 同一信息/动作/情绪被连续多段拆写 | 0-1处 | 2-3处 | ≥4处或核心场景反复出现 |
| 意义膨胀密度 | "标志着/见证/命运"等膨胀词 / 千字 | 0-1处 | 2-3处 | ≥4处 |
| 同义词循环 | 相邻段内同一事物不同称呼数 | ≤1处 | 2-3处 | ≥4处 |
| 宣传性语言 | "充满活力/引人入胜"等推销腔 / 千字 | 0处 | 1-2处 | ≥3处 |

> 以上阈值为参考值，需结合题材特点调整。例如古风题材的对话标签密度天然偏高，应适当放宽。
>
> **综合判定规则**：取五项指标中的最高档位。任一指标达重度即按重度处理；无重度时，中度指标 ≥3 项按中度处理，否则按轻度处理。

加载 [references/anti-ai-writing.md](references/anti-ai-writing.md) 的「第二部分：系统性去AI三遍法」获取完整流程。三遍法与本 skill 的关系：
- **Pass 1（去泛化）** ≈ Gate A + B + C
- **Pass 2（去书面化）** ≈ Gate B 的深化
- **Pass 3（回自然感）** ≈ Gate D + E + F
- 重度AI味执行三遍法整体处理

---

### Phase 3：逐项清除

#### ⚠️ Pitfall: 本地小模型（4B）不擅长执行润色指令

当使用 oMLX 或 LM Studio 本地小模型（如 Huihui-Qwen3.5-4B）执行去AI味润色时，模型可能输出"Let me analyze..."等推理过程而非直接输出润色结果，导致输出被污染。

**识别信号：** 输出以 "Let me..."、"I'll..." 或 "分析以下请求" 等英文/分析性开头。

**修复：** 
- **不要**依赖本地小模型做润色，它们对指令的执行方式不符合预期
- 改为直接输出模板文本（预设的文案已经很自然）
- 如果必须润色，用 deepseek-v4-flash 或 deepseek-v4-pro

#### Agent 调用：narrative-writer（去AI味执行）

Phase 2 诊断完成后，如果项目已部署 narrative-writer agent（**必须先检查 `.claude/agents/narrative-writer.md` 是否存在**），spawn `Agent(subagent_type: "narrative-writer", prompt: "项目目录：{dir}\n任务描述：去AI味\n检查范围：{待处理的正文文件}\nAI味等级：{Phase 2 诊断结果}\n处理策略：{轻度/中度/重度对应的 Gate 范围}\n模式处理：按 references/anti-ai-writing.md 的问题模式目录执行；所有新增模式都归入 Gate A-F 的对应处理。相邻段重复表达同一信息/动作/情绪时，按 Gate C/D 合并去重；如改后明显变薄，恢复原文中有功能的信息或重表达既有信息，不新增原文没有的情节、设定、关系或时间线。")` 执行去AI味操作（6 Gate + 三遍法 + 问题模式目录）。如 narrative-writer agent 未部署，由主线程直接执行。

以下为各 Gate 的详细规则（无论 agent 还是主线程执行，均须遵循）：

#### 门禁 A：禁用词替换

加载 [references/banned-words.md](references/banned-words.md)，对照禁用词表逐项检查。

**白名单机制**：

项目根目录下的 `.deslop-whitelist` 文件定义了本项目的豁免词汇（一行一个）。
如果该文件存在，其中的词汇在 Gate A 检查时被跳过，不标记为禁用词。

白名单匹配规则与禁用词检查一致：精确匹配（非子串）。

白名单适用场景：
- 命中术语（如玄幻小说中的特定术语恰好匹配禁用词）
- 角色口头禅（某角色的标志性用语）
- 世界观专有名词
- 原文刻意使用的修辞手法

如果 `.deslop-whitelist` 不存在，不强制创建文件；在报告中说明可创建该文件。只有项目目录可写且本次需要复用白名单时，才创建空白名单文件。空白名单文件等同于无白名单，所有禁用词正常标记。


**保护规则优先级**：保留创作意图与剧情功能 > 去AI Gate。Gate A-F 只能改变表达方式，不能删除伏笔、钩子、角色特征、关键信息或必要转折；遇到冲突时改为降AI重写或标注 `[需复核]`。

替换规则：
- 禁用词 → 具体动作/细节描写
- 不能简单换成另一个形容词
- 要用"展示"替代"告诉"

示例：
- ❌ "眼中闪过一丝不易察觉的悲伤" → ✅ "他垂下眼"
- ❌ "深吸一口气" → ✅ "胸口起伏了一下"（或直接删掉，这个动作90%无意义）
- ❌ "嘴角勾起一抹冷笑" → ✅ "他笑了一下，没到眼底"

#### 门禁 B：句式去套路

检测并替换以下AI高频句式：

| 句式 | 问题 | 替代方案 |
|------|------|----------|
| **"不是X，是Y"否定-纠正** | **AI最爱句式之一，单章可达20+次**。连续使用制造虚假深度感 | 改为肯定句直接陈述（"不是恐惧。是困惑"→"他眼里只有困惑"）；保留1-2处有修辞价值的，其余全部改写 |
| "...，带着..." | 万能状语，AI最爱 | 用独立短句或动作描写 |
| 陈词滥调/万能比喻 | 公式化比喻会显 AI 腔 | 换成生活化、角色化比喻或直接白描 |
| "他/她知道..." | 直接告诉读者 | 用行为展示认知 |
| 对话标签密度过高/公式化标签 | 每句都标注会机械 | 普通"说"可保留；高频或公式化时用动作/上下文替代 |
| "仿佛/犹如/宛若" | 文言腔过重 | 口语化表达或白描 |
| "不容置疑/显而易见" | 书面化判断词 | 用具体事实说话 |
| **"那种"模糊指示** | **AI填充词，制造模糊距离感**。如"那种温暖""那种感觉""那种力量" | 删除"那种"直接说名词（"那种温暖"→"温暖"），或用具体描写替代抽象指代 |

**⚠️ "不是X，是Y"是最高频AI句式**：实战数据（第243章）单章出现21次，修复后降至11次。此句式在叙事和对话中均大量出现，修复时需逐个判断修辞价值——有情感冲击力的保留（如"不是恐惧。是遗憾"），其余改写为肯定句或合并句。同一段落内超过2处"不是X是Y"必定需要改写。

#### 门禁 C：心理描写外化

AI写的心理描写特征：直接陈述情绪。

替换策略：
- "他很紧张" → "他的手在抖"
- "她很愤怒" → "她一把掀翻了桌子"
- "他很害怕" → "他的腿在发抖，几乎站不稳"
- "她很伤心" → "她转过身去，肩膀微微颤动"
- "他感到一丝失落" → "他愣了一下，把手机放回口袋"

**重复描写去重**：当相邻段反复表达同一信息、同一动作或同一情绪时，按 Gate C/D 处理，不另开专项流程。

处理方法：
- 合并同一瞬间的重复描写，保留最能推动情绪或剧情的细节
- 如果原文把一个动作拆成"动作概述 → 感知细节 → 身体反应"，改成同一段连续画面
- 若合并后节奏过快，恢复原文中有功能的信息，或把既有信息改成更自然的动作/对话表达；不在原动作后追加描写层，也不新增原文没有的情节

示例：
- ❌ "他拿起笔。手在抖。笔尖又停住。"
- ✅ "他拿起笔，笔尖刚碰到纸就偏了，手腕压了两次都没压稳。"

**⚠️ 段落级/场景级结构冗余**：AI写作常见的更深层问题是同一信息在不同段落或场景中重复出现，尤其是"分析型"场景。识别信号：

| 冗余模式 | 示例 | 处理方法 |
|----------|------|----------|
| 三段式重复 | 同一分析先简述、再列举、再详解 | 合并为一段完整分析，保留最详版本 |
| 角色A说→角色B复述→角色C总结 | 三人轮流说同一件事的不同侧面 | 保留首次完整陈述，后续改为简短确认或省略 |
| 同一场景的重复开头 | "信放在窗台上"出现两次 | 合并为一句 |
| 情绪告知→展示→再告知 | "她很愤怒"→"她的手在发抖"→"她确实很生气" | 保留展示段，删除两次告知 |

**实战案例**（第239章）：凯瑟琳分析四封信，原文写了三遍——先简述核心内容，再逐封列举，最后详细分析每封的破绽。合并为一遍完整分析后，字数减少800+且信息量无损失。

#### 门禁 D：节奏打碎

AI写作的节奏问题：句式过于整齐、段落过于匀称。

处理方法：
- 打断连续排比句（保留1-2个，删掉其余）
- 长句拆短句
- 偶尔用不完整句（口语感）
- 段落长短交错（不要每段都3-5行）

#### 门禁 E：对话去腔调

AI写的对话特征：每句话都信息完整、逻辑清晰、表达精准。

处理方法：
- 加入口语化表达（"嗯""哦""行吧"）
- 适当打断对话（角色可以答非所问）
- 用动作穿插对话（"她喝了口水。'然后呢？'"）
- 删掉解释性对话（角色不会把自己的动机说清楚）

#### 门禁 F：结尾去升华

AI写作的结尾特征：总想总结、升华、点题。

处理方法：
- 删掉总结性语句
- 用动作/场景收尾，不要用感慨收尾
- 如果结尾有"他知道...""这一刻..."→ 基本可以删

#### 门禁 G：深层模式清除（整合自 Humanizer）

> 以下规则整合自 [humanizer](../humanizer/SKILL.md) 技能的 29 种 AI 模式，针对中文网文场景做了适配。

##### G1：意义膨胀

AI 写作倾向于夸大事物的重要性，给普通事件赋予宏大意义。

| AI写法 | 问题 | 自然写法 |
|--------|------|----------|
| "标志着一个新的时代" | 普通事件被拔高 | 直接写事件本身 |
| "这是一种无声的抗争" | 赋予虚假深度 | 用具体行为展示 |
| "见证了历史的转折" | 宏大叙事腔 | 写具体发生了什么 |
| "是命运的安排" | 玄学升华 | 删掉，让情节自己说话 |
| "一切都因此改变" | 夸大影响 | 写具体改变了什么 |

**规则**：如果一句话听起来像"名人名言"或"可引用的金句"，大概率需要重写。

##### G2：刻意换词（同义词循环）

AI 的重复惩罚机制导致它不停切换同义词来指代同一事物。

**典型表现**：
- 同一角色在相邻三段内被称为"他/那个男人/对方/那人/这位年轻人"
- 同一情绪被写成"愤怒/恼火/暴怒/怒不可遏"轮番出现
- 同一地点被"那间屋子/那个房间/这处空间/此处"反复替换

**修复**：人称和指代保持一致，不要为了"不重复"而强行换词。自然的中文不怕重复"他"。

##### G3：宣传性语言

AI 倾向于用"推销腔"描写场景和人物，像在写广告文案。

| AI写法 | 问题 | 自然写法 |
|--------|------|----------|
| "充满活力的集市" | 推销腔 | "集市上人挤人" |
| "引人入胜的故事" | 夸张评价 | 直接讲故事 |
| "令人叹为观止的景色" | 旅游广告腔 | 写具体看到什么 |
| "举世闻名的大师" | 头衔堆砌 | 用具体事迹展示 |
| "无与伦比的天赋" | 绝对化夸赞 | 用行为展示天赋 |

**规则**：删除所有"评价性形容词"，用事实和行为代替。读者自己会判断好不好。

##### G4：虚假范围

AI 喜欢用"从X到Y"的结构制造虚假的广度感，但 X 和 Y 之间并没有真正的渐变关系。

**典型表现**：
- "从街头小贩到王公贵族，无人不知" → "城里人都知道"
- "从出生到死亡，他都在这个村子里" → "他一辈子没离开过这个村子"
- "从最微小的尘埃到最宏大的星辰" → 删掉，空洞

**规则**："从X到Y"结构只在 X 和 Y 确实构成有意义的范围时保留。否则改写为直接陈述。

##### G5：过度限定与模糊归因

AI 倾向于给每个判断加上过多限定词，或者把观点归因于模糊的权威。

**过度限定**：
- "或许可能大概会有一点影响" → "有影响" 或 "没什么影响"
- "在某种程度上可以说" → 直接说
- "似乎好像仿佛" → 选一个，或直接删

**模糊归因**（网文叙述中）：
- "据说" + 没有出处的信息 → 要么给出处，要么删"据说"
- "人们都说" → 谁？给具体的人
- "有传言说" → 如果后文不回收，就是废话

##### G6：先行预告与引导语

AI 喜欢先"预告"自己要说什么，而不是直接说。这在网文中表现为叙述者对读者的"过度引导"。

| AI写法 | 问题 | 自然写法 |
|--------|------|----------|
| "话说回来" | 频繁转场提示 | 直接切场景 |
| "且不说" | 假设性引导 | 直接说要说什么 |
| "让我们把目光转向" | 解说腔 | 直接写新场景 |
| "要理解这件事" | 教导腔 | 直接讲，读者自己理解 |
| "正所谓" | 引经据典腔 | 删掉，用自己的话说 |

**规则**：叙述者不需要"引导"读者。直接讲故事，信任读者的智商。

##### G7：注入灵魂（Anti-Soulless Writing）

去 AI 味不只是删除坏模式，还要注入真正的个性。"干净但没有灵魂"的文字和 AI 味一样明显。

**无灵魂写作的信号**：
- 每个句子长度和结构相同
- 没有角色个性，所有人说话方式一样
- 没有不确定感或矛盾心理
- 过于完美的逻辑链条（现实中人的想法是跳跃的）
- 读起来像百科全书或新闻稿

**注入灵魂的方法**：
- **有态度**：角色对事物有自己的好恶，不是中立旁观
- **有节奏**：短句急促、长句舒缓，混合使用
- **有矛盾**：真实的人有纠结、有犹豫、有说不清的感受
- **有跳跃**：允许思维跳跃、话题突转、不完美的过渡
- **有细节**：这个场景特有的、别的场景不会出现的细节

---

### Phase 3.5：质量评分（可选）

去 AI 味完成后，可用以下 5 维度评分框架评估润色质量（50 分制）：

| 维度 | 满分 | 评分标准 |
|------|:----:|----------|
| **直接性** | 10 | 是否删掉了空洞的铺垫和限定，直接说事？ |
| **节奏** | 10 | 句子长短是否交错？段落是否有呼吸感？ |
| **信任度** | 10 | 是否信任读者的智商，不手把手解释？ |
| **真实性** | 10 | 读起来是否像真人写的，不像 AI 组装的？ |
| **精炼度** | 10 | 是否删掉了所有废话，每句话都有功能？ |

- **40+ 分**：自然流畅，可直接发布
- **30-39 分**：有少量 AI 痕迹，轻度修改即可
- **20-29 分**：中度 AI 味，需要再过一遍
- **<20 分**：重度 AI 味，需要重新润色

---

### Phase 4：输出润色结果

```
## 去AI味润色报告

### 修改统计
- 总修改数：{N} 处
- 禁用词替换：{N} 处（Gate A）
- 句式调整：{N} 处（Gate B）
- 心理外化：{N} 处（Gate C）
- 节奏调整：{N} 处（Gate D）
- 对话优化：{N} 处（Gate E）
- 结尾修正：{N} 处（Gate F）
- 深层模式清除：{N} 处（Gate G：膨胀/换词/宣传/虚假范围/过度限定/预告/灵魂注入）

### 质量评分
- 直接性：{N}/10
- 节奏：{N}/10
- 信任度：{N}/10
- 真实性：{N}/10
- 精炼度：{N}/10
- **总分：{N}/50**

### 修改前后对比
{逐段展示修改，标注改动类型}

### 润色后全文
{完整输出润色后的文本}
```

---

## 深度审查：委托大莉M去AI味

当用户要求"让大莉看看"或需要深度去AI味审查时，使用以下工作流：

### 流程

```
莉莉丝初筛（快速扫描AI味+无关内容）
  → delegate_task 给大莉M（mimo-v2.5-pro）
  → 大莉M输出：审查报告 + 完整修改版全文
  → 莉莉丝用 write_file 覆盖原文件
  → 汇报修改摘要给冰哥
```

### delegate_task 调用模板

```python
delegate_task(tasks=[{
    "goal": "你是网文深度审校专家。请对以下章节进行两项审查：\n"
            "1. AI味检测：逐段扫描，找出所有AI写作痕迹（模板化比喻、重复句式、"
            "过度解释、过渡句、拟人化套路等），标注行号和具体问题。\n"
            "2. 无关内容检测：检查是否有与上下文矛盾、角色来源不明、或与前文设定冲突的内容。\n"
            "3. 输出修改版：基于以上审查，输出完整的修改后章节文本（直接可发布的完整Markdown），"
            "修改所有发现的AI味和无关内容，保持原文风格和情节不变。\n"
            "请用中文输出审查报告和修改版全文。",
    "context": "以下是需要审查的章节全文：\n\n{章节全文}\n\n---\n背景信息：\n{背景设定}",
    "toolsets": ["file"]
}])
```

### 关键注意事项

- **必须传完整章节全文**到 context，不要让大莉M自己去读文件（子agent没有父会话的文件路径上下文）
- **必须传背景信息**（世界观、角色关系、前文关键设定），否则大莉M可能误判无关内容
- **大莉M输出的修改版通常质量很高**，可直接用 write_file 覆盖原文件
- **覆盖前简要检查**修改版是否有明显问题（如删掉了关键情节）
- **汇报时列出主要修改项**（AI味核心问题、无关内容发现），让冰哥快速了解改动

### 常见AI味模式（大莉M审查重点）

| 模式 | 示例 | 出现频率 |
|------|------|---------|
| 「不是X，是Y」否定-纠正句式 | "不是训练的整齐。是节奏被锁死的整齐。" | 极高（单章可达21次） |
| 「短暂的空白」过渡句 | "短暂的空白。" | 经典AI标记 |
| 模板化比喻 | "像睡着了还在呼吸的东西" | 高 |
| 比喻堆叠 | 同段两个"像…" | 中 |
| 过度解释 | "那种气味当过兵的人闻得出来" | 中 |
| 拟人化套路 | "很短暂。又睡着了。" | 中 |
| 「那种」模糊指示词 | "那种温暖""那种力量""那种感觉" | 高（单章可达7次，删"那种"即可修复） |
| 意义膨胀（Gate G1） | "标志着一个新时代""命运的安排" | 中 |
| 刻意换词（Gate G2） | 同一角色3段内3种称呼 | 中 |
| 宣传性语言（Gate G3） | "充满活力的集市""引人入胜" | 低-中 |

---

## 批量扫描（Automated Bulk Scan）

当需要检查 **大量章节（10+ 章）** 的 AI味 时，逐章手动审查效率太低。改用 Python 脚本批量扫描：

**策略**：只执行 Gate A（禁用词替换）+ Gate C（重复描写差异化），跳过 B/D/E/F——大莉写的文本在这四个维度几乎无问题。

### 批量扫描脚本模板

```python
import os, re

path = "/path/to/chapters"
banned = ["仿佛","犹如","宛若","一丝","一抹","些许","突然","缓缓","不禁","微微",
          "眼中闪过","嘴角勾起","眉头微皱","心中一动","不由得"]

for f in sorted(os.listdir(path)):
    if not f.endswith('.md'): continue
    fp = os.path.join(path, f)
    with open(fp) as fh:
        content = fh.read()
    hits = []
    for w in banned:
        for m in re.finditer(re.escape(w), content):
            ctx = content[max(0,m.start()-15):min(len(content),m.end()+15)].replace('\n',' ')
            hits.append((w, ctx))
    if not hits:
        print(f"✅ {f} — 干净")
    else:
        print(f"\n⚠️  {f} ({len(content)}字)")
        for w, ctx in hits[:5]:
            print(f"    [{w}] ...{ctx}...")
        if len(hits) > 5:
            print(f"    ...还有 {len(hits)-5} 处")
```

### 修复脚本模板

```python
# 对发现的每个问题做针对性替换
targets = {
    "第004章_标题.md": [
        ("手在微微发抖", "手在抖"),
        ("深吸一口气", "吸了口气"),
    ],
}
for fn, fixes in targets.items():
    fp = os.path.join(path, fn)
    with open(fp) as fh:
        content = fh.read()
    for old, new in fixes:
        content = content.replace(old, new)
    with open(fp, 'w') as fh:
        fh.write(content)
```

### 场景判断

| 场景 | 方法 | 耗时 |
|------|------|:----:|
| 1~3 章 | 手动逐章审查 | 2-5 分钟 |
| 4~9 章 | 批量扫描 → 手动修复 | 1 分钟 |
| **10+ 章** | **批量扫描 → 批量修复** | **30 秒** |

---

## 使用场景

| 场景 | 操作 |
|------|------|
| 用户贴一段文字说"太AI了" | 执行完整检测 + 润色流程 |
| 用户说"帮我润色" | 先检测AI味，再润色 |
| 用户说"检查下有没有AI味" | 只做检测，不做修改 |
| 用户在写作过程中 | 嵌入式提醒（不改原文，只标注） |

---

## 参考资料

按需加载以下文件：

| 文件 | 何时加载 |
|------|----------|
| [references/banned-words.md](references/banned-words.md) | 检测和替换禁用词时 |
| [references/anti-ai-writing.md](references/anti-ai-writing.md) | **去AI味完整指南**：预防+三遍法+范例 |
| [references/verified-replacements.md](references/verified-replacements.md) | 实战验证的替换规则：45章网文去AI味的禁用词表、结尾去升华模式、比喻堆叠坍塌规则、心理外化模式等 |
| [humanizer](../humanizer/SKILL.md) | **门禁 G 的来源**：29种通用AI模式检测（英文原版），含意义膨胀、刻意换词、宣传性语言等深层模式 |

---

## 完整发布前 QA 流水线

当用户要求将小说章节发布到平台（如番茄小说）前，推荐的完整 QA 流程：

1. **🚀 大莉剧情审查** → 用 delegate_task 派大莉审查剧情连贯性、角色一致性、设定逻辑、bug
2. **⚡ 闪莉修复** → 闪莉根据审查报告逐项修复 bug
3. **🧹 闪莉去AI味** → 用本技能（story-deslop）做全量去AI味处理
4. **📤 发布** → 用 fanqie-publisher 发布到番茄小说

**注意**：闪莉修复 bug 和去AI味是**两步独立操作**。修 bug 只改剧情逻辑，去AI味只改表达方式，不要在一次任务中混做。

### 参考文件
- [references/pre-publish-pipeline.md](references/pre-publish-pipeline.md) — 完整的 QA 检查清单

## 流程衔接

**流水线：** 通用
**位置：** 润色（共享收尾）

| 时机 | 跳转到 | 命令 |
|---|---|---|
| 继续写作 | story-long-write / story-short-write | `/story-long-write` 或 `/story-short-write` |
| 发现结构问题 | story-long-analyze / story-short-analyze | `/story-long-analyze` 或 `/story-short-analyze` |
| 准备做封面 | story-cover | `/story-cover` |

---

## 语言

- 跟随用户的语言回复，用户用什么语言就用什么语言回复
- 中文回复遵循《中文文案排版指北》
